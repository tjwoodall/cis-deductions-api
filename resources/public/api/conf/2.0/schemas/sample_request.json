{
  "title": "Sample Endpoint",
  "description": "Sample Endpoint",
  "type": "object",
  "properties": {
    "data": {
      "description": "a field defined within the schema",
      "type": "string",
      "example": "example"
    }
  },
  "required": [
    "data"
  ]
}